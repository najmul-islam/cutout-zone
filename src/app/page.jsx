import Hero from "@/components/home/hero/Hero";
import { Box } from "@mui/material";

const HomePage = () => {
  return (
    <Box>
      <Hero />
    </Box>
  );
};
export default HomePage;

export const metadata = () => {
  return { title: "Cut Out service | image excellance service provider" };
};
